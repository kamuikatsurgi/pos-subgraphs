{
  "name": "amoy-subgraph-polygon",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ amoy-subgraph-polygon",
    "create-local": "graph create --node http://localhost:8020/ amoy-subgraph-polygon",
    "remove-local": "graph remove --node http://localhost:8020/ amoy-subgraph-polygon",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 amoy-subgraph-polygon",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": { "matchstick-as": "0.6.0" }
}
